invl main()
    list l = [1, 2, 3, 4]
    list r = [9, 8, 7]
    int min
with
    call f(l, r, min)

invl f(list l, list r, int min)
    if size(l) < size(r)
    then min += size(l)
    else min += size(r)
    fi size(l) < size(r)
with
    l <=> r
