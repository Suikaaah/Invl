invl main()
    list l = [7, 8, 9]
    list r = [1, 2, 3 ,4]
    int temp
with
    call f(l, r, temp)

invl f(list l, list r, int temp)
    temp += size(l)
with
    for x to temp
        skip
    end
    l <=> r
